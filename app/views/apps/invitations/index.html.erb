<%= render Content::Component.new do %>

	<%= render PageHeader::Component.new(title: I18n.t('settings.team.invitations')) do %>
		<%= link_to( t('settings.team.add_new'), 
			new_app_invitation_path(@app.key), 
			data: {
			'turbo-frame': 'modal', 
			'turbo-action': 'replace', 
			'action': 'click->modal#open'
			}, 
			class: "btn btn-flat-dark md") 
		%>
	<% end %>

	<%= turbo_frame_tag 'agents-table', data: {controller: 'history-frame'} do %>
		<%= render 'table' %>
	<% end %>
	
<% end %>