<%= render Content::Component.new do %>

	<%= turbo_frame_tag 'app-settings', data: {controller: 'history-frame'} do %>

		<%= render PageHeader::Component.new(title: I18n.t('task_bots.title') ) do %>
		<% end %>

		<%= render Tabs::Component.new(	items: bots_menu_data ) do %>
			<% #= render Hints::Component.new(type: "articles")%>
			<% #= render 'table'%>
			<%= case controller.action_name
					when 'users' then render('users_form')
					when nil, 'leads' then render('leads_form')
					end
			%>
		<% end %>

	<% end %>
	
<% end %>